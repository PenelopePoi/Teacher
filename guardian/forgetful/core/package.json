{
    "name": "@guardian/forgetful-core",
    "version": "0.1.0",
    "description": "PII/PHI scrubber core. Detects, redacts, and emits a signed manifest of what was removed (types + counts, never values). Forgetful by construction.",
    "private": true,
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "files": [
        "dist/**/*"
    ],
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "test": "jest",
        "test:watch": "jest --watch",
        "test:coverage": "jest --coverage",
        "lint": "eslint src test --ext .ts",
        "typecheck": "tsc -p tsconfig.json --noEmit"
    },
    "dependencies": {
        "zod": "3.23.8"
    },
    "devDependencies": {
        "@types/jest": "29.5.12",
        "@types/node": "20.11.24",
        "jest": "29.7.0",
        "ts-jest": "29.1.2",
        "typescript": "5.4.5"
    },
    "engines": {
        "node": ">=20"
    }
}
