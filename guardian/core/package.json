{
    "name": "@guardian/core",
    "version": "0.1.0",
    "description": "Pure-TS Love Invariant evaluator, agency envelope, hash-chained WAL, consent verification. Zero Theia/AWS dependencies.",
    "private": true,
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "files": [
        "dist/**/*"
    ],
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "test": "jest",
        "test:watch": "jest --watch",
        "test:coverage": "jest --coverage",
        "lint": "eslint src test --ext .ts",
        "typecheck": "tsc -p tsconfig.json --noEmit"
    },
    "dependencies": {
        "zod": "3.23.8"
    },
    "devDependencies": {
        "@types/jest": "29.5.12",
        "@types/node": "20.11.24",
        "fast-check": "3.19.0",
        "jest": "29.7.0",
        "ts-jest": "29.1.2",
        "typescript": "5.4.5"
    },
    "engines": {
        "node": ">=20"
    }
}
